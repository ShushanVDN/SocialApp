import React from 'react'
import preloader from '../../../images/802.gif'

let Preloader = (props) => {
    return <div>
                 <img src={preloader} />
            </div>

}

export default Preloader 